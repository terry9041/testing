// Example program
#include <iostream>
using namespace std;
struct node
{
    double data;
    node* link;
};

void selectionSort(node* &head)
{
    node * temp = head;
    int length = 0;
    while (temp != nullptr)
    {
        length++;
        temp = temp -> link;
    }
    for (int i = 0; i < length; i++)
    {
        currentVal = 
        minVal = head->data;
        
}

int main()
{
  
}